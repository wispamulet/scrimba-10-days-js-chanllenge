/*
* Day 6: Enclose in Brackets
* In this challenge, you'll:

* Given a string, enclose it in round brackets.

* Example
* For inputString = "abacaba", the output should be encloseInBrackets(inputString) = "(abacba)".
*/

const encloseInBrackets = (str: string): string => `(${str})`;
